import styled from "@emotion/styled";

const Wrapper = styled.div`
  height: 50px;
  background-color: orange;
  // font-family: myfont;
`;

export default function Navigation() {
  return <Wrapper>navigation영역</Wrapper>;
}
